name: "Test: Flaky Random"

on:
  workflow_dispatch:

jobs:
  test-flaky-random:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: Absterrg0/execforge-runtime/start@v1
        env:
          EXECFORGE_API_TOKEN: ${{ secrets.EXECFORGE_API_TOKEN }}
          EXECFORGE_API_URL: ${{ secrets.EXECFORGE_API_URL }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run flaky-random test
        env:
          CI: true
        run: |
          node --test \
            --test-reporter=spec --test-reporter-destination=stdout \
            --test-reporter=junit --test-reporter-destination=junit-results.xml \
            test/flaky-random.test.js

      - uses: Absterrg0/execforge-runtime/finish@v1
        if: always()
        env:
          EXECFORGE_API_TOKEN: ${{ secrets.EXECFORGE_API_TOKEN }}
          EXECFORGE_API_URL: ${{ secrets.EXECFORGE_API_URL }}
          EXECFORGE_JOB_STATUS: ${{ job.status }}
